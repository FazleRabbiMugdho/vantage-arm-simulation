# This file intentionally has nothing required for the core pipeline.
# The core app (Phases 1-5, 100% of rubric) runs entirely client-side
# with zero backend dependencies. These keys are only needed for the
# optional Phase 3B agentic bonus (Prompt 14).
#
# Copy this file to .env.local and fill in your keys:
# GEMINI_API_KEY=
# GROQ_API_KEY=
